╔══════════════════════════════════════════════════════════════════════╗
║                   🚨 EMERGENCY RECOVERY CARD 🚨                      ║
║              If Face Unlock Breaks Your Login                        ║
╚══════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────┐
│ METHOD 1: Emergency Script (If you can still login)                  │
└──────────────────────────────────────────────────────────────────────┘
  $ sudo ./emergency_disable.sh


┌──────────────────────────────────────────────────────────────────────┐
│ METHOD 2: TTY Console (Press Ctrl+Alt+F2)                            │
└──────────────────────────────────────────────────────────────────────┘
  Login with password, then run:
  
  $ sudo systemctl stop faceunlock.service
  $ sudo systemctl disable faceunlock.service
  $ sudo sed -i 's/^auth.*pam_faceunlock.so/#&/' /etc/pam.d/common-auth
  
  Press Ctrl+Alt+F1 to return to GUI


┌──────────────────────────────────────────────────────────────────────┐
│ METHOD 3: Recovery Mode (At boot: Hold Shift → Advanced → Recovery)  │
└──────────────────────────────────────────────────────────────────────┘
  Select "root shell", then run:
  
  # mount -o remount,rw /
  # sed -i 's/^auth.*pam_faceunlock.so/#&/' /etc/pam.d/common-auth
  # rm -f /usr/lib/security/pam_faceunlock.so
  # reboot


┌──────────────────────────────────────────────────────────────────────┐
│ METHOD 4: Live USB                                                    │
└──────────────────────────────────────────────────────────────────────┘
  Boot Live USB, then:
  
  $ sudo mount /dev/sdXY /mnt     # Replace XY with your partition
  $ sudo sed -i 's/^auth.*pam_faceunlock.so/#&/' /mnt/etc/pam.d/common-auth
  $ sudo rm -f /mnt/usr/lib/security/pam_faceunlock.so
  $ sudo umount /mnt && sudo reboot


┌──────────────────────────────────────────────────────────────────────┐
│ 🔍 QUICK DIAGNOSIS                                                   │
└──────────────────────────────────────────────────────────────────────┘
  Check if active:
    $ systemctl is-active faceunlock.service
    $ grep pam_faceunlock /etc/pam.d/*


┌──────────────────────────────────────────────────────────────────────┐
│ 📂 PAM Configuration Files by Distribution                           │
└──────────────────────────────────────────────────────────────────────┘
  Ubuntu/Debian:  /etc/pam.d/common-auth
  Fedora/Arch:    /etc/pam.d/system-auth
  Also check:     /etc/pam.d/sudo
                  /etc/pam.d/gdm-password
                  /etc/pam.d/sddm


┌──────────────────────────────────────────────────────────────────────┐
│ ⚡ PREVENTION                                                         │
└──────────────────────────────────────────────────────────────────────┘
  BEFORE enabling on login:
  
  ✓ Test with sudo commands first
  ✓ Test lock screen  
  ✓ Verify password still works in TTY (Ctrl+Alt+F2)
  ✓ Keep SSH enabled as backup
  ✓ Know how to access recovery mode


╔══════════════════════════════════════════════════════════════════════╗
║  💾 SAVE THIS FILE! Print it or keep it accessible on another device ║
║  📖 Full guide: RECOVERY.md                                          ║
╚══════════════════════════════════════════════════════════════════════╝
